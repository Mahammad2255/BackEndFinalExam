@model IEnumerable<Color>
@{
    ViewData["Title"] = "Index";
}

<div class="col-lg-2 h3 mb-4 text-gray-800">
    <a asp-controller="Color" asp-action="Create" asp-area="Manage" class="btn btn-primary">Create</a>
</div>
<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>

        </tr>
    </thead>
    <tbody>
        @foreach (Color color in Model)
        {
            <tr>

                <th scope="row">@color.Id</th>

                <th scope="row">@color.Name</th>

                <td>
                    <a class="btn btn-primary" asp-action="Detail" asp-area="manage" asp-route-id="@color.Id">Detail</a>
                    <a class="btn btn-warning" asp-action="Update" asp-area="manage" asp-route-id="@color.Id">Update</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-area="manage" asp-route-id="@color.Id">Delete</a>

                </td>





            </tr>


        }
    </tbody>
</table>